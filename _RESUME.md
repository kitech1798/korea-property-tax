# 재개 지점 — 2026-08-13

## 지금 상태

```
659 tests passed · 룰셋 45규칙 린트 통과 · 상황 시뮬레이션 125건 전량 통과
정부 골든 17값 전부 유지
```

## 이번에 한 일 — 임대차를 도메인에 들였다

손으로 적은 상담 메모(양도세·상생임대·세입자 통보)를 엔진이 못 푸는 이유를 찾으니
**임대차 사실 자체가 도메인에 없었다.** `RentalRegistration`은 등록임대사업자
등록정보이지 전월세 계약이 아니다. 거주(`ResidenceSpell`)만 있고 임대가 없으니
상생임대 특례도, 세입자 통지기한도, 갱신요구권도 원리적으로 판정할 수 없었다.

**① 도메인 `LeaseSpell` + `LeaseOrigin`** (models.py)
  개시·종료·체결일·보증금·월세·계약금 증빙·퇴거일. 결론 필드는 두지 않는다.
  `LeaseOrigin` 5값 — 신규/**승계**/묵시적갱신/갱신요구권/합의재계약.
  승계를 구분하는 이유는 §155의3①1호 괄호가 "취득으로 임대인 지위가 승계된
  계약은 제외"하기 때문이다. bool 하나로는 표현이 안 된다.

**② 룰셋 2건** (transfer/house.yaml, 44→45규칙)
  `transfer.sangsaeng_lease` 신설 · `transfer.house_count_specials` 트랙 분리 후
  조정대상지역 일시적2주택 3년→2년 단축 추가.

**③ 엔진 `sangsaeng.py`** + `periods.full_months` + transfer_tax 연결
  비과세(§154①)와 장특공제(§159의4) 두 곳의 거주요건을 면제한다.

**④ 테스트 +94** (test_lease / test_sangsaeng / test_sangsaeng_rules / test_sangsaeng_e2e)

## ★★ 이번에 확정한 것 — 면제되는 것은 '제한'이지 거주기간이 아니다

§155의3①이 지목하는 §159의4는 장특공 표2 대상을 "거주기간이 2년 이상인 것"으로
정의한다(2026-08-13 법제처 XML 확인). 면제되는 것은 **표2 진입 요건**이고,
표2 안의 거주기간 공제율은 실제 거주기간으로 계산한다. 의제 규정은 없다.

→ 실거주 0년 상생임대주택의 장특공제는 개편안 아래에서
  **'27년 40% → '28년 20% → '29년 0%**로 무너진다. 특례가 살아 있어도 그렇다.
  **비과세 12억은 지켜지지만 장기보유특별공제는 지켜지지 않는다.**
  `test_sangsaeng_e2e.py::test_장특공제는_실거주_0년으로_계산된다`가 이걸 잠근다.

## 정부 원문 2종이 어긋난 지점 (기록 목적)

일시적2주택 단축의 경과조치를 **상세본 p.73은 "주택·주택을 취득할 수 있는 권리"**로,
개조식 p.21은 "주택"으로만 적는다. 상세본이 분양권·입주권을 포함해 더 넓다.
넓은 쪽이 납세자에게 유리하지만 분양권 취득일을 입력받지 않으므로 판정하지 않는다.

## 4단계 — 매도시점 최적화기 (완료, 673 tests)

**`reference.lease_renewal_notice` 룰셋**(46규칙) — 주임법 §6①·§6의2·§6의3.
세액을 계산하지 않고 **언제까지 무엇을 해야 하는가**를 낸다.

**`engine/sell_window.py`** — 기존 `strategy.sell_timing`과 다른 두 가지
  ① 연이 아니라 **날짜**다. 기한이 2028-01-31이면 "2028년"이라는 답은 틀렸다.
  ② 세액과 제약을 **분리**한다. `best`(팔 수 있는 날 중 최소) vs `naive_best`
     (제약 무시 최소), 그 차이가 `constraint_cost`다.

★ **후보 날짜를 격자로만 뽑지 않는다.** 세액은 날짜에 대해 계단 함수이고 값이
  바뀌는 곳은 법이 그은 경계뿐이다. 각 연도 1/1·12/31, 각 제약의 기한일, 그리고
  **기한 다음 날**을 표본에 직접 넣는다. 격자만 쓰면 1월 31일과 2월 1일 사이의
  절벽을 통째로 놓친다.

실측(양도 32억·취득 12억·보유 10년·거주 0년 가정):
```
최적 2027-02-01  3.06억
절벽 2027-12-31 → 2028-01-01  +1.16억  (장특공 보유공제 4%→2%)
절벽 2028-01-31 → 2028-02-01  +3.96억  (상생임대 특례 소멸)
절벽 2028-12-31 → 2029-01-01  +0.99억  (보유공제 폐지)
갱신 시나리오: 최적이 2029-02-01로 밀리고 9.16억 — 통지 실패 비용 6.10억
```

### 이 단계에서 잡은 결함 3건

1. **상생임대 판정이 양도일을 안 봤다.** 계약서의 미래 임대기간까지 앞당겨 세어,
   2026년에 팔아도 2년을 채운 것으로 읽었다. §155의3①은 "요건을 모두 갖춘 주택을
   양도하는 경우"다 → `_lease_months(spell, as_of=양도일)`.
2. **양도기한을 구간 시작일로 해석했다.** 개편안은 '26.10.1. 이후 양도분부터이므로,
   오늘이 시행일 전이면 기한이 통째로 사라졌다. 기한은 '언제 파느냐'에 붙는다.
3. **격자가 월초가 아니었다.** 8월 13일·9월 13일… 같은 날짜가 화면에 나온다.

## 검증 단계 — 시뮬레이션·반증·스트레스 (2026-08-13)

```
686 tests · 룰셋 46규칙 · 시나리오 137건(+12) · 스트레스 16,216건 충돌 0
```

**① 하네스에 임대차를 들였다** — `sim/spec.py`에 `leases:` 필드.
   `down_payment_evidenced`를 안 적으면 **모름(None)**이다. 빠뜨린 것을 '확인됨'으로
   읽으면 요건 미충족 사건이 통과해 불변식 검사가 통째로 무의미해진다.

**② 불변식 3종 추가** — `sangsaeng_never_raises_tax`(면제가 세금을 늘릴 수 없다),
   `sangsaeng_deadline_direction`(기한 다음 날이 더 쌀 수 없다),
   `sell_window_contract`(최적안은 팔 수 있는 날 · 제약은 하한 · **기한이 표본에 있을 것**).

**③ ⭐반증 테스트 `tests/test_invariants_falsifiable.py`** — 이 프로젝트는 "정의상
   항상 0이라 절대 안 울리는 가짜 검수"를 만든 적이 있다. 통과 137건은 엔진이 옳다는
   증거일 수도, 검사기가 죽어 있다는 증거일 수도 있다. 그래서 엔진을 한 곳씩
   **일부러 뒤집어** 각 불변식이 우는지 확인했다(변이 5종 + 거짓양성 6종).
   ⚠️ `sell_window_renewal_not_cheaper`는 현재 구현에서 **위반이 나올 수 없다**
      (갱신은 선택지를 줄이기만 한다). 미래 회귀용 가드임을 테스트에 명시했다.

### 스트레스 16,200건이 잡은 결함 2건 — 둘 다 '빈칸에 혜택'

1. **보증금·월세가 모두 0이면 증가율 0%로 읽어 요건을 통과시켰다.**
   대가가 없으면 임대차가 아니라 사용대차이고, 실제로는 금액 미입력이 대부분이다.
2. **기간이 겹치는 두 계약을 직전/상생 쌍으로 삼았다.**
   §155의3①1호의 '직전'은 바로 앞이다. 겹치면 앞뒤 관계가 성립하지 않는데,
   3년짜리 계약 하나가 직전계약으로 인정돼 1년 6개월 요건을 통과했다.

   둘 다 이제 **판정 불가**로 낸다. 반증 테스트를 짜다 검사기의 사각도 하나 찾았다 —
   `_sell_window_contract`는 양도일부터 표본을 뜨므로, 양도일이 임대차 종료 이후면
   '막힌 날짜'가 구간에 없어 제약 불변식이 울릴 데이터를 못 받는다.

## 다음에 할 것

- **5단계 화면** — 임대차 입력 UI + 시나리오 타임라인. 엔진은 다 됐고 화면만 없다.
- 임차인 `tenant_ref`를 안 받으면 갱신요구권 소진을 판정할 수 없다. 입력에 필요.
- 주임법 §6의3① 단서 1~9호(거절 사유)는 전부 사실관계라 판정하지 않고 안내만 한다.

---

# 재개 지점 — 2026-08-05 (2차)

## 지금 상태

```
565 tests passed · 룰셋 44규칙 린트 통과 · 상황 시뮬레이션 125건 전량 통과
정부 골든 17값 전부 유지 (재산세 5 · 종부세 6 · 양도세 6)
배포 github.com/kitech1798/korea-property-tax → korea-property-tax.streamlit.app
```

```powershell
cd "c:/Users/admin/Desktop/교육/부동산 상담"
python -m pytest tests/ -q                    # 565건
python -m realestate_tax.rules.lint           # 룰셋 44규칙
python -m sim.run                             # 상황 시뮬레이션 125건
./tools/deploy.ps1                            # 4개 게이트 통과해야 푸시
streamlit run app.py --server.port 8555
```

키는 User 환경변수 + `.streamlit/secrets.toml`(gitignore) 양쪽에 있다.

## 오늘 한 일

**① 상황 시뮬레이션 하네스(`sim/`)** — 상황을 YAML로 진술받아 엔진 전 구간에
통과시키고 **LLM 없이** 불변식 14종으로 판정한다. 어제 감사는 지적 66건 중 46건이
기각됐는데(코드를 읽고 추측했으므로), 1차 심판을 실행 결과로 바꾸니 반박의 여지가
없어졌다. 에이전트 2회차로 시나리오 125건 생성. 배포 게이트에 물려 있다.

**② 버그 11건 수정 (SIM-01~10 + 전략 가드)**
같은 병이 **여섯 번** 났다 — *모델에 있는 사실을 엔진이 안 읽는다.*
거주 여부 → 거주기간 → 취득일 → Election → 실거주 가드 → 인정 사유.
가장 비쌌던 것은 SIM-06: 양도세가 취득일을 안 읽어 비과세·장특공제·세율·중과완화가
**한꺼번에** 무너졌다. 개편안 간판 전략인 '27~'28 매도 창구 이득이 0원으로 나왔다
(실측 차이 2.99억). 그 밖에 세목 오염(SIM-07, 0원 → 1.7억), 윤년 민법 §160③(SIM-08),
광역시 지역요건(SIM-09), §154⑧ 통산(SIM-10).

**③ 절세 방안 5종** — 이 서비스의 비어 있던 절반.
  ① 배우자 증여(회수기간·이월과세 경고·**1주택자는 손해**)
  ② '27~'28 매도 창구  ③ 납부유예  ④ 거주 인정 사유  ⑤ 합산배제 신고
  전부 **비용·부작용을 같은 화면에** 낸다. 절감액만 보여주면 조언이 아니라 유인이다.

**④ 양도세 주택수 특례** — §155① 일시적2주택, §155② 상속주택.

## 다음에 할 것

`docs/미구현_목록.md`에 **이유별로** 정리돼 있다. 요약하면:

- **입력만 붙이면 되는 것** — §155④⑤ 합가·혼인(합가일), §154⑧ 통산(멸실주택 취득일),
  배우자 증여 10년 내 기공제액. 조문은 다 확보했다.
- **데이터가 없어 못 하는 것** — 지방 세컨드홈 지역 목록(행안부 고시),
  소형신축·준공후미분양 요건, 재산세 특례세율 일몰(부칙이 API로 안 온다).
- **검증 공백** — 공시가격 자동조회 실측(`tools/verify_hub.py`).

## ⚠️ 모노레포 주의

`교육` 저장소는 여러 프로젝트를 담고 **git 인덱스를 공유**한다.
다른 세션이 스테이징해 둔 파일이 있으면 `git add -A .`로 범위를 좁혀도
`git commit`이 그것까지 함께 커밋한다 — 2026-08-05 실제 발생(커밋 835dff7에
공모전 파일 4개가 섞였다).

**공개 배포에는 새지 않는다** — `tools/deploy.ps1`이 `HEAD:부동산 상담` 서브트리만
떼어 푸시하기 때문이다. 그래도 이력이 섞이니 커밋 전 `git status`를 확인하는 편이 낫다.

## ⚠️ 법제처 API 사용법

- **반드시 `type=XML`** — JSON·HTML은 계산식과 표를 **이미지로** 준다.
  부담부증여 안분식, 증여세 세율표를 이것 때문에 못 볼 뻔했다.
- **부칙은 어떤 형식으로도 안 온다.** 한시규정 유효기간이 부칙에만 있어
  그 종류의 누락은 현재 경로로 검출 불가능하다.

---

## 이전 기록 (1차)

# 재개 지점 — 2026-08-05

## 지금 상태

```
451 tests passed · 룰셋 35규칙 린트 통과 · 상담 지식 16건
정부 골든 17값 전부 유지 (재산세 5 · 종부세 6 · 양도세 6)
화면 4탭: 상황입력 / 보유세·4년타임라인 / 팔까버틸까 / 상담
```

```powershell
cd "c:\Users\admin\Desktop\교육\부동산 상담"
python -m pytest tests/ -q                    # 오프라인 451건
python -m realestate_tax.rules.lint           # 룰셋 검사
python tools/build_advisory.py --check        # 상담 지식 스키마 검증
python -m pytest tests/ -m live -q            # 실호출 (약 4분, 키 필요)
streamlit run app.py --server.port 8555
```

환경변수 2개는 User 스코프에 설정돼 있다: `JUSO_CONFM_KEY`, `DATA_GO_KR_KEY`.

⚠️ **세제개편안 PDF 5종은 git에 없다**(저장소 루트 `.gitignore`의 `*.pdf`).
`세제개편안_8.3/` 폴더는 로컬에만 있고, **골든 테스트의 정답지가 이 PDF에서 나온다.**
다른 기기에서 재개하려면 PDF를 먼저 옮겨야 한다.

---

## 이번 세션에 한 일

### 1. 주소 → 공시가격 자동조회 완성

juso 승인키가 발급되어 사슬이 이어졌다.

```
"압구정로 113"
  → juso addrLinkApi : 법정동코드 + 지번         (1초)
  → 건축HUB 표제부    : 단지 확인 + 동 목록      (0.2초)
  → 건축HUB 전유부    : 그 동의 호 목록          (2.7초)
  → 건축HUB 주택가격  : 공시가격                 (41초, 캐시 24h)
```

라이브 테스트 4건 통과. 실측으로 잡은 함정 2가지는 코드 주석과 메모리에 남겼다
(**지번이 두 군데 있고 서로 다름**, **산여부 규약이 두 필드에서 반대**).

### 2. 멀티에이전트 감사 반영 — 이 프로젝트 최대 사건

에이전트 32개 · 지적 66건 · 300만 토큰 · 66분.
**반박에서 46건(70%) 기각** → 생존 20건 → **전부 내가 정부 원문으로 재확인 후** 반영.

전문: `docs/audit/2026-08-04_감사.md`

가장 큰 것 세 가지:

| 결함 | 영향 |
|---|---|
| 1세대1주택 비과세에 보유·거주 요건이 없었음 | 보유 6개월 사례에서 **2.29억을 0원으로 안내** |
| 세부담상한이 세액을 0으로 붕괴 | 2,344만원 소실 |
| 개편안 과세문턱(14억/9억) 규칙 부재 | 과세대상 아닌 사람에게 과세 |

**넣지 않은 것 1건**: F03(재산세 특례세율 일몰). 에이전트가 인용한 부칙을
법제처 조문 API로 재현하지 못했다. 확인 못 한 것은 넣지 않는다.

---

### 3. 상담 지식 16건 + 상담 탭 (완료)

`corpus/advisory/*.yaml` 4파일 16건. **손으로 옮기지 않고** `tools/build_advisory.py`가
초안 JSON에서 생성한다 — 조문 인용이 촘촘해 사람이 옮기면 전사 오류가 난다
(이번 감사의 F01이 정확히 그 종류였다). 사람이 하는 일은 **조문을 원문과 대조하는 것**이고,
종부령 §5의2⑧ · 종부세법 §10의2② · 지방세령 §110의2②③ · 소득세법 §97의2①을 확인했다.

고치려면 JSON을 고치고 다시 생성한다. YAML을 직접 고치면 다음 생성에서 덮인다.

**이 작업 중에 잡은 버그 2개** — 지식을 붙이자 엔진의 구멍이 드러났다:

1. `_resides`가 `ResidenceSpell`을 **아예 읽지 않았다.** 도메인이 1급 엔티티로 둔 사실을
   엔진이 무시해, 거주 이력을 입력해도 옵션을 따로 주지 않으면 비거주로 계산됐다.
   기본공제 14억 vs 9억 — 5억이 갈리는 자리다.
   *'모르면 보수적으로'와 '알아도 무시'는 다른 것이다.*
2. 상담 계층이 기본공제 노드의 **화면 문구를 파싱해** 거주 여부를 되찾고 있었다.
   그 문구는 '개편안 + 1세대1주택'일 때만 붙어서 부부공동명의에서 조용히 False가 됐고,
   거주 중인 사람에게 "비거주 1주택" 조언이 나갔다.
   → `JongbuseResult.resides`로 **값을 내보낸다.** 표시용 문자열은 계산 결과의 저장소가 아니다.

---

## 다음 할 일 (우선순위)

### 1. Streamlit Cloud 배포 — ✅ 완료
juso 승인키는 **배포 URL 기준으로 등록돼 있다**(2026-08-13 신청 내역 확인).

    URL(IP)  https://korea-property-tax.streamlit.app
    시스템명  시뮬레이터 개발 · 서비스망 인터넷망

⚠️ 이 자리에 "집 IP 기준으로 발급받았으니 재등록해야 한다"는 **낡은 할 일**이 남아
있었다. 이미 처리된 일을 다음 세션이 다시 하도록 시키고 있었고, 더 나쁜 것은
그 문장이 **자택 IP를 본문에 적어 두었다는 점**이다. 그 커밋이 공개 저장소로
나갔다(2026-08-13 발견·이력 세탁). 자세한 것은 `tools/deploy.ps1` 머리말.

교훈은 두 줄이다.
  · 끝난 할 일은 지운다. 남아 있으면 다음 사람이 없는 문제를 푼다.
  · **메모에 개인정보를 적지 않는다.** 이 파일은 커밋되고, 커밋은 지워도 이력에 남는다.

### 2. `tools/fetch_law.py`에 부칙 수집 경로 추가 → F03 확인
지금은 `eflawjosub` 타깃이라 **부칙을 원리적으로 못 본다.**
한시 규정의 유효기간은 조문이 아니라 부칙에만 적히므로,
같은 유형의 누락을 검출할 수 없다. **개별 버그가 아니라 구조적 구멍이다.**

### 3. 잔여 세목
- 부담부증여
- 소형신축·준공후미분양·인구감소지역 특례
  (조문은 확보됨 — 종부령 §4의3③3호 바목 1)~4))

---

## 이 프로젝트에서 지키는 것

1. **에이전트 지적을 그대로 믿지 않는다.** 반박 통과 = "둘이 동의" ≠ "법이 그렇다".
   반영 전에 정부 원문(법제처 조문 API / 개편안 PDF)으로 직접 확인한다.
2. **확인 못 한 것은 넣지 않는다.** 그럴듯한 것보다 없는 게 낫다.
3. **판정 불가는 실제로 답이 갈릴 때만 낸다.** 답이 같은 지점에서 판정 불가를 내는 것은
   정직이 아니라 무능이다(비과세 요건 수정 중 골든 6값을 깨뜨리며 배웠다).
4. **테스트가 안 통과하면 테스트부터 고치지 않는다.** 절반은 전제가 틀린 것이다
   (단기세율 비교에서 실제로 그랬다).
5. **세액이 아니라 표를 고정한다.** 세액은 우연히 맞을 수 있지만 표는 아니다.
6. **표시용 문자열은 계산 결과의 저장소가 아니다.** 판정은 값으로 내보낸다.
   화면 문구를 파싱해 불리언을 되찾으면, 문구가 붙는 조건이 바뀔 때 조용히 깨진다.
7. **도메인이 모델링한 사실을 엔진이 읽는지 확인한다.** `ResidenceSpell`은 처음부터
   있었는데 엔진이 안 읽었다. 엔티티를 만드는 것과 쓰는 것은 다른 일이다.
