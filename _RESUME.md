# 재개 지점 — 2026-08-05

## 지금 상태

```
451 tests passed · 룰셋 35규칙 린트 통과 · 상담 지식 16건
정부 골든 17값 전부 유지 (재산세 5 · 종부세 6 · 양도세 6)
화면 4탭: 상황입력 / 보유세·4년타임라인 / 팔까버틸까 / 상담
```

```powershell
cd "c:\Users\admin\Desktop\교육\부동산 상담"
python -m pytest tests/ -q                    # 오프라인 451건
python -m realestate_tax.rules.lint           # 룰셋 검사
python tools/build_advisory.py --check        # 상담 지식 스키마 검증
python -m pytest tests/ -m live -q            # 실호출 (약 4분, 키 필요)
streamlit run app.py --server.port 8555
```

환경변수 2개는 User 스코프에 설정돼 있다: `JUSO_CONFM_KEY`, `DATA_GO_KR_KEY`.

⚠️ **세제개편안 PDF 5종은 git에 없다**(저장소 루트 `.gitignore`의 `*.pdf`).
`세제개편안_8.3/` 폴더는 로컬에만 있고, **골든 테스트의 정답지가 이 PDF에서 나온다.**
다른 기기에서 재개하려면 PDF를 먼저 옮겨야 한다.

---

## 이번 세션에 한 일

### 1. 주소 → 공시가격 자동조회 완성

juso 승인키가 발급되어 사슬이 이어졌다.

```
"압구정로 113"
  → juso addrLinkApi : 법정동코드 + 지번         (1초)
  → 건축HUB 표제부    : 단지 확인 + 동 목록      (0.2초)
  → 건축HUB 전유부    : 그 동의 호 목록          (2.7초)
  → 건축HUB 주택가격  : 공시가격                 (41초, 캐시 24h)
```

라이브 테스트 4건 통과. 실측으로 잡은 함정 2가지는 코드 주석과 메모리에 남겼다
(**지번이 두 군데 있고 서로 다름**, **산여부 규약이 두 필드에서 반대**).

### 2. 멀티에이전트 감사 반영 — 이 프로젝트 최대 사건

에이전트 32개 · 지적 66건 · 300만 토큰 · 66분.
**반박에서 46건(70%) 기각** → 생존 20건 → **전부 내가 정부 원문으로 재확인 후** 반영.

전문: `docs/audit/2026-08-04_감사.md`

가장 큰 것 세 가지:

| 결함 | 영향 |
|---|---|
| 1세대1주택 비과세에 보유·거주 요건이 없었음 | 보유 6개월 사례에서 **2.29억을 0원으로 안내** |
| 세부담상한이 세액을 0으로 붕괴 | 2,344만원 소실 |
| 개편안 과세문턱(14억/9억) 규칙 부재 | 과세대상 아닌 사람에게 과세 |

**넣지 않은 것 1건**: F03(재산세 특례세율 일몰). 에이전트가 인용한 부칙을
법제처 조문 API로 재현하지 못했다. 확인 못 한 것은 넣지 않는다.

---

### 3. 상담 지식 16건 + 상담 탭 (완료)

`corpus/advisory/*.yaml` 4파일 16건. **손으로 옮기지 않고** `tools/build_advisory.py`가
초안 JSON에서 생성한다 — 조문 인용이 촘촘해 사람이 옮기면 전사 오류가 난다
(이번 감사의 F01이 정확히 그 종류였다). 사람이 하는 일은 **조문을 원문과 대조하는 것**이고,
종부령 §5의2⑧ · 종부세법 §10의2② · 지방세령 §110의2②③ · 소득세법 §97의2①을 확인했다.

고치려면 JSON을 고치고 다시 생성한다. YAML을 직접 고치면 다음 생성에서 덮인다.

**이 작업 중에 잡은 버그 2개** — 지식을 붙이자 엔진의 구멍이 드러났다:

1. `_resides`가 `ResidenceSpell`을 **아예 읽지 않았다.** 도메인이 1급 엔티티로 둔 사실을
   엔진이 무시해, 거주 이력을 입력해도 옵션을 따로 주지 않으면 비거주로 계산됐다.
   기본공제 14억 vs 9억 — 5억이 갈리는 자리다.
   *'모르면 보수적으로'와 '알아도 무시'는 다른 것이다.*
2. 상담 계층이 기본공제 노드의 **화면 문구를 파싱해** 거주 여부를 되찾고 있었다.
   그 문구는 '개편안 + 1세대1주택'일 때만 붙어서 부부공동명의에서 조용히 False가 됐고,
   거주 중인 사람에게 "비거주 1주택" 조언이 나갔다.
   → `JongbuseResult.resides`로 **값을 내보낸다.** 표시용 문자열은 계산 결과의 저장소가 아니다.

---

## 다음 할 일 (우선순위)

### 1. Streamlit Cloud 배포
⚠️ **juso 승인키를 배포 URL로 재등록**해야 한다. 최초 발급은 집 IP 기준으로 받았다.
(다른 IP에서도 호출은 됐지만 약관상 보장이 아니다 — 가이드 3.1이 URL/IP 기준 부여를 명시한다.)
재등록: business.juso.go.kr → 신청 내역 → 시스템 URL을 배포 주소로 변경.

### 2. `tools/fetch_law.py`에 부칙 수집 경로 추가 → F03 확인
지금은 `eflawjosub` 타깃이라 **부칙을 원리적으로 못 본다.**
한시 규정의 유효기간은 조문이 아니라 부칙에만 적히므로,
같은 유형의 누락을 검출할 수 없다. **개별 버그가 아니라 구조적 구멍이다.**

### 3. 잔여 세목
- 부담부증여
- 소형신축·준공후미분양·인구감소지역 특례
  (조문은 확보됨 — 종부령 §4의3③3호 바목 1)~4))

---

## 이 프로젝트에서 지키는 것

1. **에이전트 지적을 그대로 믿지 않는다.** 반박 통과 = "둘이 동의" ≠ "법이 그렇다".
   반영 전에 정부 원문(법제처 조문 API / 개편안 PDF)으로 직접 확인한다.
2. **확인 못 한 것은 넣지 않는다.** 그럴듯한 것보다 없는 게 낫다.
3. **판정 불가는 실제로 답이 갈릴 때만 낸다.** 답이 같은 지점에서 판정 불가를 내는 것은
   정직이 아니라 무능이다(비과세 요건 수정 중 골든 6값을 깨뜨리며 배웠다).
4. **테스트가 안 통과하면 테스트부터 고치지 않는다.** 절반은 전제가 틀린 것이다
   (단기세율 비교에서 실제로 그랬다).
5. **세액이 아니라 표를 고정한다.** 세액은 우연히 맞을 수 있지만 표는 아니다.
6. **표시용 문자열은 계산 결과의 저장소가 아니다.** 판정은 값으로 내보낸다.
   화면 문구를 파싱해 불리언을 되찾으면, 문구가 붙는 조건이 바뀔 때 조용히 깨진다.
7. **도메인이 모델링한 사실을 엔진이 읽는지 확인한다.** `ResidenceSpell`은 처음부터
   있었는데 엔진이 안 읽었다. 엔티티를 만드는 것과 쓰는 것은 다른 일이다.
